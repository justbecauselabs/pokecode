# Server Configuration
NODE_ENV=development
PORT=3001
LOG_LEVEL=info

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=claude_code_mobile
DB_USER=postgres
DB_PASSWORD=

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Claude Configuration - OPTIONAL
# Leave empty/commented to use local Claude Max account (no billing)
# Set only if you want to use API key billing instead
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Claude Code CLI Path - REQUIRED for worker functionality
# Path to the Claude CLI executable
CLAUDE_CODE_PATH=/Users/billy/.claude/local/claude

# File Storage
FILE_STORAGE_BASE=/var/claude-code/projects

# Rate Limiting
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# CORS - Use specific origins when credentials are enabled, not '*'
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://williams-macbook-pro-1.tailaf1649.ts.net:5173